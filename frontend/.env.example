# Chief of Staff API base URL (no trailing slash). Used by the chat UI for POST /chat.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Clerk (https://dashboard.clerk.com → API Keys)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=
